Create/refresh a dedicated 3D view with a Section Box around elements
picked from a linked model. Works from any view type (plan, section,
elevation, 3D) and automatically opens the 3D view when done.

Usage:
- Ctrl+Click one or more elements inside a linked model, then run the tool.
- If nothing is pre-selected, the tool will prompt you to pick elements.